# Per-project /goal runtime artifacts — never commit
.claude/
.claude/goal.json
.claude/goal-hook.log
.claude/goal.pause
.claude/goal-baseline-*
.claude/goal-events.jsonl
.claude/goal-otel-cursor
.claude/MIGRATED_TO_GOAL

# v2 runtime state directory — never commit
.goal/

# Internal design docs / RFCs / plans — kept local, never committed
proposal/

# IDE
.vscode/
.idea/

# Logs
*.log

# Node / TypeScript build artifacts
mcp/node_modules/
mcp/dist/
bin/node_modules/

# Profiling / coverage artifacts
*.profraw

# macOS
.DS_Store
